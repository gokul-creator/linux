useradd with uid and different home directory 
sudo useradd  -m <directory> -u uid -g gid
groupadd <gorupname>
useradd <name> -g <groupname>
adduser with no login shell--> adduser <name> -s /sbin/nologin


git tas1
git init --bare <directory.git>

